exports = (typeof window === 'undefined') ? global : window;

exports.stringsAnswers = {
  reduceString: function(str, amount) {
  return str.splice(2,amount);
  },
  wordWrap: function(str, cols) {

  },
  reverseString: function(str) {

  }
};
